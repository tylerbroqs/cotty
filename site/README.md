# Cotty landing page

A self-contained static page (single `index.html`, no build step, no
external requests) for https://orlixai.xyz/cotty.

To deploy, copy it into the website repository as `cotty/index.html`:

```sh
cp site/index.html <orlix-repo>/cotty/index.html
```

It also works from any static host, at any path.
