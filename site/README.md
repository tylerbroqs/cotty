# Cotty landing page

A self-contained static page (single `index.html`, no build step, no
external requests) meant to live on the project website. Drop it into the
website repository as-is, or serve it from any static host.
